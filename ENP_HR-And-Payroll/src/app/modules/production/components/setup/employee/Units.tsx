import { SetupComponent } from '../CommonSetupComponent'

const Units = () => {

  const data = {
    title: 'Unité',
    url: 'Units',
  }
  return (
    <div>
      < SetupComponent data={data} />    
    </div>
  )
}

export { Units }

